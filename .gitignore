node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
*.db
.claude/
*.traineddata
!public/tessdata/kor.traineddata
!public/tessdata/eng.traineddata

# AI 모델 파일 — best.pt/best.onnx는 git에 포함 (Render 배포용)
server/models/*.weights
server/models/*.onnx.data
# 변환 중간 파일 제외
server/models/best_*.pt
server/models/*_temp.*
.history/
_test_in.json
metadata.json
server/models/best.onnx
logs/
# Vercel 링크 (Render 배포 예정)
.vercel

# 실데이터 xlsx가 소스 트리에 유입되는 것 방지 (엑셀 파일은 uploads/ 로만)
src/*.xlsx
src/sample/*.xlsx

# 2026-08-25 · .omx (로컬 metrics/logs · 프로덕션 무관)
.omx/

# 게시판 업로드 이미지
uploads/board/

# 🔐 Google Service Account · Drive 통합 (git 커밋 절대 금지)
src/keys/
keys/

# 🔐 2026-08-12 · 테넌트 시스템 설정 파일 (env 오버라이드 · KEY 포함)
server/tenant.config.json
