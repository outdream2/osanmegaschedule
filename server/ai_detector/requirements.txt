# AI 탐지 에이전트 의존성
# 설치: pip install -r server/ai_detector/requirements.txt

# 핵심 (필수)
ultralytics>=8.3.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pillow>=10.0.0
numpy>=1.26.0

# Detection/Segmentation 강화 (선택 — 없으면 일부 기능 제한)
opencv-python>=4.10.0

# 이미 설치된 경우 무시됨
# onnxruntime>=1.20.0  # ultralytics 없을 때 fallback용
